[toolchain]
# Pinned to a known-good stable: upstream uses unsigned `is_multiple_of`
# (stabilized in 1.87) and `ruint@1.18` requires rustc >= 1.90, so the
# system "stable" alias may be too old. Bump deliberately.
channel = "1.94"
components = ["rustfmt", "clippy"]
