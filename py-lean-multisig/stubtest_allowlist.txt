# maturin auto-generates `__init__.py` that does
# `from .py_lean_multisig import *`, exposing the compiled .so as a submodule
# of the same name. We don't separately stub that submodule because all of
# its symbols are already covered by the top-level stub via the re-export.
py_lean_multisig\.py_lean_multisig
